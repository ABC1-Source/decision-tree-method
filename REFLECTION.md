# REFLECTION · 复盘（为什么这个 skill 长这样）

> 原理层文档：SKILL.md 告诉你"怎么用"，本文件告诉你"为什么这么设计"——从 8/15 一整天打磨解三角形示范件的实战记录提炼。**修改/扩展本 skill 前先读这里**，避免重踩已踩过的坑。
> 一句话：这个 skill 不是设计出来的，是被打回来的。

---

## 一、演进轨迹（v1→v9，一次一个教训）

| 版本 | 我做了什么 | 被打回的原因 | 固化的纪律 |
|---|---|---|---|
| v1-v3 | 拿模型记忆写解三角形决策树 | "你一个数学教辅都没翻就蒸馏完了？" | **资料先于记忆**：禁模型碎片，先翻考纲/教材 |
| v4 | 翻 21cnjy 一轮讲义 + 51jiaoxi，学"10 题型/考情/易错/课本典例"结构 | "不够，去翻别人的教辅书、一轮复习资料、知识提纲" | **知识体系要对照教辅知识梳理项数**（漏块=返工） |
| v5 | 用户点名翻一数，学到"模块→节→类型"三级 + 角的取舍单列 + 化角/化边拆分 | "B站博主一数的教辅找找看" | **同类型教辅要交叉，不只一家** |
| v6 | 补知识体系 7→11 块（测量术语/高公式/中线角平分线公式/正切恒等式） | "知识点就这些？" | **知识体系=教辅完整版，不是记忆碎片** |
| v7 | 抓朱昊鲲《基础2000》+ 学科网专题03，四源交叉，做验证表 | "同类型教辅也要去抓" | **至少三源交叉**（用户钦定标准） |
| v8 | 用户纠正"打法不一样"，从教辅骨架改成**模型树**（分类→排除→收敛） | "我们走的模型树，你验证过模型树思维效率高" | **组织骨架=方法理念，不是教辅结构** |
| v9 | 用户拷问"按教辅内容框架就这些？"，逐条对照补 3 处硬伤 | "按照那些教辅的内容来说，你这个框架就这些？" | **先穷举后建模**：建模前必须列全教辅技能点清单 |
| 指令 | 把全流程写成给执行窗口的操作手册 | "全过程思路给出，从第一次翻资料开始整理，不要遗漏" | **流程要可复制**：每阶段带产出/反模式/自查 |

**总模式**：每一版都是被用户"拷问"出来的，不是自己设计出来的。拷问顺序恰好是正确方法论的正确顺序——资料 → 结构 → 交叉 → 理念 → 穷举 → 可复制。

---

## 二、五个最贵的教训（改本 skill 前必读）

### 1. 先穷举后建模（最贵，v8→v9）
- 错误：拿模型框架去套教辅（"我有什么模型→往里填"），导致漏掉**单本独有**的技能点——比例问题（朱昊鲲4.4单列）、周长（学科网题型6单列）、外接圆半径（学科网方法技巧），全是"某本单独列了而我没单列"的。
- 正确：**先逐本列全技能点清单 → 并集 → 标"单列"标记 → 再建模**。模型候选表 = 技能点全集，一个不漏。
- 检查：建模前先问"穷举清单过完了吗？"——没过完不准建模。

### 2. 组织骨架 = 方法理念，不是教辅结构（v8）
- 错误：拿一数的"模块→节→类型"当组织骨架——那是**内容骨架**，不是打法。
- 正确：模型树（分类→排除→收敛）才是组织方式，教辅只是"收集"阶段的输入。教辅告诉你**有什么料**，模型树告诉你**怎么走**。

### 3. 单列 = 危险信号（v5/v7）
- 某本教辅把某个技能点**单独列成节/题型**（朱昊鲲4.3余弦变型、4.4比例问题、学科网题型6周长、一数"角的取舍"），说明作者认为它值得独立训练——**这类最容易漏，必须全部进模型**。
- 反向验证：四本里只有一数把"角的取舍"单列——说明它值得单列（50分学生丢分最集中点）。

### 4. 知识体系要"全"，不是"有"（v6）
- 错误：知识体系列到"够用"就停，漏了测量术语（仰俯角/方位角/坡比）、高公式、中线长公式。
- 正确：对照教辅"知识梳理"的项数标准自查——范本 02 节 11 块，少一块都是返工。

### 5. 能出题 = 可调用（贯穿）
- 每个模型/候选/知识块，检验标准是"能不能拿它产出一道题"。能出题=真资产；不能=概念层，删。

---

## 三、模型树格式（v9 定稿，全系列统一模板）

```
[模块名]（根）
├─ M0 入口分流 → 拿到题它问什么？
├─ M1 [核心候选模型] → 候选表（=技能点全集）→ 排除 → 收敛唯一
├─ M2 [必经关卡]（⚠ 高失分点，画成关卡不是分支）
├─ M3... 下游模型（按 M0/M1 收敛结果进入）
└─ 选学模型（标"90 分后/暂不碰"）
```

每个模型统一格式：
```
### 模型 Mx · 名称（触发条件）
| 候选 | 适用条件 | 排除条件 |
**收敛**：条件命中的唯一候选 = 路径。执行动作：...
**易错**：...
**采分点**：...
```

**关卡 ≠ 分支**：高失分点（解三角形=角的取舍）画成"必经关卡"，从候选①连红线，不是并列分支——这是"它值得单列"的结构化表达。

---

## 四、资料区间（锁死，区间外不翻）

教育模块只翻四源教辅：**一数《必刷100讲·基础》/《高考数学核心方法》· 朱昊鲲《真题全刷·基础2000》· 21cnjy 一轮讲义 · 学科网专题**。
锚点 = 人教 A 版教材 + 高考考纲 + 近 3 年真题。
**区间外不翻**：非教育类/大学数学/泛方法论一律不采。

---

## 五、给"下一个模块"（数列等）的执行清单

1. 阶段0：锁资料区间，抓四源目录
2. 阶段1：穷举技能点（并集+单列标记+★/▲判定）——**核心，没做完不准建模**
3. 阶段2：知识体系（对照范本 02 节 11 块标准，公式列全）
4. 阶段3：模型树（M0→M1候选表→M2关卡→下游→选学，候选表=技能点全集）
5. 阶段4：交叉验证表（由阶段1生成）+ 考情/技巧/易错/分层/课本典例
6. 验收：穷举无遗漏 / 引用不出区间 / 真题能跑通 / 证据结论分文件

详细操作见 `examples/高中数学/模型树蒸馏全流程指令.md`。

---

## 六、本 skill 的设计哲学（为什么是"模型树"不是"决策树"）

- **决策树**（初版形态）：按特征分流，"已知 X → 走 Y 路"——适合信息充分时的快速路由。
- **模型树**（最终形态）：**候选表全列 → 逐条排除 → 收敛唯一**——适合"需要判断哪个方法适用"的场景（应试/学习/决策的本质）。
- 区别一句话：决策树是"路牌"，模型树是"筛选器"。应试/学习/决策里，真正难的不是"知道路"，是"判断该走哪条路"——所以模型树效率更高（经验证）。
- 延伸：本理念适用于一切"多个候选方法选一个"的场景（学新技能、分析问题、做决策），不只是应试。

---

> 复盘结束语：这个 skill 的每一行都带着"被打回来的原因"。改它之前，先问自己——我要加的东西，是教辅/教材/真题里有的，还是我记忆里有的？后者一律不采。
